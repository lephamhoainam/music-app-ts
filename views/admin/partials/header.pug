header.header 
    .container-fluid 
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href=`/${prefixAdmin}/dashboard`) Admin 

            .col-9 
                div.text-right 
                    a.btn.btn-sm.btn-a(href=`/${prefixAdmin}/my-account`) #{user.fullname}
                    a.btn.btn-sm.btn-b(href=`/${prefixAdmin}/auth/logout`) Đăng xuất
