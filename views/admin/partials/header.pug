header.header 
    .container-fluid 
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href=`/${prefixAdmin}/dashboard`) Admin 

            .col-9 
                div.text-right 
                    a.btn.btn-outline-light.btn-sm.mr-1(href=`/${prefixAdmin}/my-account`) #{user.fullname}
                    a.btn.btn-outline-warning.btn-sm(href=`/${prefixAdmin}/auth/logout`) Đăng xuất